apiVersion: apps/v1
kind: Deployment
metadata:
  name: secubot-ms
spec:
  replicas: 1
  selector:
    matchLabels:
      app: secubot-ms
  template:
    metadata:
      labels:
        app: secubot-ms
    spec:
      containers:
      - name: secubot-ms
        image: your-registry/secubot-ms:latest
        imagePullPolicy: IfNotPresent
        ports:
        - containerPort: 8000
        envFrom:
        - configMapRef:
            name: secubot-ms-config
        - secretRef:
            name: secubot-ms-secrets
        readinessProbe:
          httpGet:
            path: /health
            port: 8000
          initialDelaySeconds: 20
          periodSeconds: 10
          timeoutSeconds: 3
          failureThreshold: 3
        livenessProbe:
          httpGet:
            path: /health
            port: 8000
          initialDelaySeconds: 45
          periodSeconds: 20
          timeoutSeconds: 3
          failureThreshold: 3
        resources:
          requests:
            cpu: 100m
            memory: 128Mi
          limits:
            cpu: 500m
            memory: 512Mi
